# 墨流（InkFlow）0.4.0 工作清单

本文用于让项目内的协作状态可见；它不改变小说正文、正史或数据库。

## 已完成并已验证

- [x] 扩展显示名为 `inkflow墨流`，版本为 0.4.0。
- [x] 在 VS Code 与桌面端支持选中文字后留批注；批注不修改正文。
- [x] 对未验收草稿支持“只修订选区”：保留旧草稿版本，不自动验收，也不写入正史。
- [x] 选区以 Unicode 代码点计数，避免 emoji 等字符造成前后端选区偏移。
- [x] 工作区级自定义写作预设，支持新增、同名更新与确认删除。
- [x] 助手回复默认逐字淡入，速度为 10 字/秒，并可立即显示；开启系统“减少动态效果”时直接显示。
- [x] “查看判断摘要与过程”只展示可复核的约束、依据和工具状态；不展示或保存原始思维链。
- [x] Writer、规划与 Reviewer 提示词加入场景推进、因果、时间线与人物知识边界检查。
- [x] 完成 Python、桌面端和 VS Code 扩展的构建与定向验证；发布包已生成并记录校验值。

## 按当前决定暂停

- [ ] 继续扩展墨宝的宠物互动；现有动作与对话仍可用，但不把它作为下一阶段重点。
- [ ] 生成或内置独立的 720p 演示视频；发布包不包含这类演示视频、HTML 演示页或视频帧。现有墨宝短动作素材仅服务于侧栏互动，需要时再以独立素材制作，不影响扩展功能。

## 等待外部操作

- [ ] 上传到 VS Code Marketplace。扩展包位于 `artifacts/extension/`；最终公开发布前仍需项目账户持有人确认。

## 下一轮建议

- [ ] 在真实小说项目中做一次人工体验走查：选区批注、局部修订、新版本审查、验收和正史提交。
- [ ] 收集实际长章节下的首屏加载与逐字显示体验，再决定是否拆分 Monaco 编辑器代码块。

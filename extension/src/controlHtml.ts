import * as vscode from "vscode";
import { randomUUID } from "node:crypto";

type Mood = "idle" | "thinking" | "waiting" | "success" | "rest" | "welcome" | "reading";

export function controlHtml(
  webview: vscode.Webview,
  extensionUri: vscode.Uri,
  revealCharactersPerSecond: number,
): string {
  const nonce = randomUUID().replaceAll("-", "");
  const revealRate = Math.max(1, Math.min(60, revealCharactersPerSecond));
  const asset = (name: string) => webview.asWebviewUri(vscode.Uri.joinPath(extensionUri, "media", "mascot", name)).toString();
  const clips: Record<Exclude<Mood, "reading">, { video: string; poster: string }> = Object.fromEntries(
    ["idle", "thinking", "waiting", "success", "rest", "welcome"].map((mood) => [
      mood,
      { video: asset(`mobao-${mood}.webm`), poster: asset(`mobao-${mood}.poster.png`) },
    ]),
  ) as Record<Exclude<Mood, "reading">, { video: string; poster: string }>;
  const assets = JSON.stringify({ clips, reading: asset("mobao-reading.png") }).replaceAll("<", "\\u003c");
  const settings = JSON.stringify({ revealCharactersPerSecond: revealRate });

  return `<!doctype html>
<html lang="zh-CN"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0">
<meta http-equiv="Content-Security-Policy" content="default-src 'none'; img-src ${webview.cspSource} data:; media-src ${webview.cspSource}; style-src ${webview.cspSource} 'unsafe-inline'; script-src 'nonce-${nonce}';">
<style>
  :root{color-scheme:light dark;--panel:var(--vscode-sideBar-background);--soft:var(--vscode-sideBarSectionHeader-background);--line:var(--vscode-widget-border,rgba(127,127,127,.28));--muted:var(--vscode-descriptionForeground);--accent:var(--vscode-focusBorder);--danger:var(--vscode-errorForeground);--radius:10px}
  *{box-sizing:border-box}html,body{height:100%;margin:0}body{overflow:hidden;color:var(--vscode-foreground);background:var(--panel);font-family:var(--vscode-font-family);font-size:12px}button,textarea,input{font:inherit}button{border:1px solid var(--line);border-radius:6px;padding:7px 9px;color:var(--vscode-foreground);background:var(--vscode-button-secondaryBackground);cursor:pointer}button:hover{background:var(--vscode-button-secondaryHoverBackground)}button.primary{border-color:var(--vscode-button-border,transparent);color:var(--vscode-button-foreground);background:var(--vscode-button-background)}button.primary:hover{background:var(--vscode-button-hoverBackground)}button:focus-visible,textarea:focus-visible,input:focus-visible,summary:focus-visible{outline:1px solid var(--accent);outline-offset:2px}button:disabled{opacity:.52;cursor:default}.icon-button{width:27px;height:27px;padding:0;border-color:transparent;background:transparent}.shell{display:flex;flex-direction:column;height:100%;min-height:0}.top{position:relative;display:grid;grid-template-columns:minmax(0,1fr) 92px;gap:8px;min-height:116px;padding:12px 10px 8px;border-bottom:1px solid var(--line);background:linear-gradient(145deg,var(--soft),transparent)}.eyebrow{margin:0 0 4px;color:var(--muted);font-size:9px;font-weight:700;letter-spacing:.14em}.top h1{margin:0;font-family:var(--vscode-editor-font-family),serif;font-size:18px;line-height:1.2}.top-copy{min-width:0}.top-copy>p:last-child{margin:7px 0 0;color:var(--muted);font-size:10px;line-height:1.5}.companion{position:relative;display:grid;place-items:center;min-height:88px}.pet-halo{position:absolute;width:76px;height:76px;border-radius:50%;background:radial-gradient(circle,rgba(241,142,50,.18),transparent 68%);filter:blur(2px)}.pet-media{position:relative;width:88px;height:88px;object-fit:contain;filter:drop-shadow(0 8px 15px rgba(0,0,0,.23));transition:transform .2s ease}.pet-media:hover{transform:translateY(-2px) scale(1.03)}.pet-speech{position:absolute;z-index:3;right:72px;top:4px;width:min(168px,52vw);padding:7px 8px;border:1px solid var(--line);border-radius:8px 8px 2px 8px;background:var(--vscode-editorWidget-background,var(--panel));box-shadow:0 5px 15px rgba(0,0,0,.18);font-size:10px;line-height:1.4}.pet-speech[hidden]{display:none}.pet-actions{grid-column:1/-1;display:flex;gap:5px;overflow-x:auto;padding-top:5px;scrollbar-width:none}.pet-actions::-webkit-scrollbar{display:none}.pet-actions button{flex:0 0 auto;padding:4px 7px;border-radius:999px;color:var(--muted);background:transparent;font-size:9px}.content{display:flex;flex:1;min-height:0;flex-direction:column}.scroll{flex:1;min-height:0;overflow-y:auto;padding:9px 9px 2px;scrollbar-gutter:stable}.empty{padding:18px 4px}.empty p{color:var(--muted);line-height:1.55}.empty button{width:100%;margin-top:7px}.status{margin-bottom:9px;padding:9px;border:1px solid var(--line);border-radius:var(--radius);background:var(--soft)}.status-head{display:flex;align-items:center;justify-content:space-between;gap:8px}.status-head>div{min-width:0}.status strong,.status small{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.status strong{font-size:12px}.status small{margin-top:3px;color:var(--muted);font-size:9px}.metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:1px;margin-top:8px;border:1px solid var(--line);background:var(--line)}.metric{min-width:0;padding:6px 4px;background:var(--panel);text-align:center}.metric span,.metric b{display:block;overflow:hidden;text-overflow:ellipsis}.metric span{color:var(--muted);font-size:8px;white-space:nowrap}.metric b{margin-top:2px;font-size:11px}.section-title{display:flex;align-items:center;justify-content:space-between;margin:12px 1px 7px}.section-title h2{margin:0;font-size:11px}.section-title span{color:var(--muted);font-size:9px}.messages{display:flex;flex-direction:column;gap:9px}.message{display:grid;grid-template-columns:24px minmax(0,1fr);gap:7px;align-items:start}.avatar{display:grid;place-items:center;width:23px;height:23px;border:1px solid var(--line);border-radius:6px;color:#df9138;background:var(--soft);font-family:serif;font-size:10px}.message.user .avatar{color:var(--vscode-charts-blue)}.bubble{min-width:0;padding:8px 9px;border:1px solid transparent;border-radius:3px 9px 9px 9px;background:var(--soft)}.message.user .bubble{border-color:var(--line);background:transparent}.message.error .bubble{border-color:var(--danger)}.message-text{margin:0;line-height:1.58;white-space:pre-wrap;overflow-wrap:anywhere}.message details{margin-top:7px;border-top:1px solid var(--line);padding-top:6px;color:var(--muted)}.message summary{cursor:pointer;font-size:9px}.reasoning-list{margin:7px 0 0;padding-left:16px}.reasoning-list li{margin:4px 0;line-height:1.45}.message pre{max-height:180px;overflow:auto;margin:7px 0 0;padding:7px;border-radius:5px;background:var(--vscode-textCodeBlock-background);font-family:var(--vscode-editor-font-family);font-size:9px;white-space:pre-wrap}.reveal-char{display:inline-block;animation:charIn .18s ease-out both}.skip-reveal{margin-top:6px;padding:3px 6px;border-color:transparent;background:transparent;color:var(--muted);font-size:9px}.activity{margin:9px 0;padding:8px;border-left:2px solid var(--accent);background:var(--vscode-textBlockQuote-background)}.activity-head{display:flex;align-items:center;gap:7px}.pulse{width:6px;height:6px;border-radius:50%;background:var(--accent);box-shadow:0 0 8px var(--accent);animation:pulse 1.2s infinite}.activity strong{font-size:10px}.timeline{margin:7px 0 0;padding:0;list-style:none;color:var(--muted);font-size:9px}.timeline li{display:grid;grid-template-columns:10px 1fr;gap:4px;margin-top:4px;line-height:1.4}.timeline li::before{content:'·';color:var(--accent);font-weight:700}.selection{margin-top:10px;padding:9px;border:1px solid color-mix(in srgb,var(--accent) 46%,var(--line));border-radius:var(--radius);background:color-mix(in srgb,var(--accent) 6%,transparent)}.selection-head{display:flex;align-items:center;justify-content:space-between;gap:8px}.selection-head strong{font-size:10px}.selection-head span{overflow:hidden;color:var(--muted);font-size:8px;text-overflow:ellipsis;white-space:nowrap}.quote{max-height:72px;overflow:auto;margin:7px 0;padding:7px;border-left:2px solid #df9138;background:var(--vscode-textBlockQuote-background);font-family:var(--vscode-editor-font-family);font-size:10px;line-height:1.5;white-space:pre-wrap}.chips{display:flex;gap:5px;overflow-x:auto;margin:6px 0;scrollbar-width:none}.chips button{flex:0 0 auto;padding:4px 6px;border-radius:999px;background:transparent;color:var(--muted);font-size:8px}textarea,input{width:100%;border:1px solid var(--vscode-input-border,var(--line));border-radius:6px;padding:8px;color:var(--vscode-input-foreground);background:var(--vscode-input-background)}textarea{resize:vertical;line-height:1.5}.selection textarea{min-height:62px}.selection-actions{display:flex;justify-content:flex-end;gap:5px;margin-top:6px}.selection-note{margin:6px 0 0;color:var(--muted);font-size:8px;line-height:1.4}.tray{margin-top:10px;border:1px solid var(--line);border-radius:var(--radius);background:var(--soft)}.tray>summary{padding:8px 9px;cursor:pointer;font-size:10px;font-weight:600}.tray-body{padding:0 8px 8px}.preset-list{display:flex;flex-wrap:wrap;gap:5px}.preset{display:inline-flex;align-items:center;gap:3px}.preset>button:first-child{padding:5px 7px}.preset .delete{width:22px;height:22px;padding:0;border-color:transparent;background:transparent;color:var(--muted)}.new-preset{display:grid;grid-template-columns:86px 1fr auto;gap:5px;margin-top:7px}.new-preset button{padding-inline:8px}.workflow{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:5px}.workflow button{text-align:left}.workflow button small{display:block;margin-top:2px;color:var(--muted);font-size:8px}.composer{flex:0 0 auto;margin:7px 9px 9px;padding:7px;border:1px solid var(--line);border-radius:10px;background:var(--vscode-editor-background);box-shadow:0 -8px 24px rgba(0,0,0,.1)}.composer textarea{min-height:66px;max-height:170px;border:0;padding:4px;background:transparent;box-shadow:none}.composer-foot{display:flex;align-items:center;justify-content:space-between;gap:6px;margin-top:5px}.composer-foot span{color:var(--muted);font-size:8px}.composer-actions{display:flex;gap:5px}.stop{border-color:var(--danger);color:var(--danger);background:transparent}.tool-row{display:grid;grid-template-columns:repeat(3,1fr);gap:4px}.tool-row button{padding:5px;color:var(--muted);font-size:8px}.notice{margin:7px 1px 1px;color:var(--muted);font-size:8px;line-height:1.4}
  @keyframes pulse{0%,100%{opacity:.35}50%{opacity:1}}@keyframes charIn{from{opacity:0;filter:blur(2px);transform:translateY(2px)}to{opacity:1;filter:none;transform:none}}
  @media(max-width:275px){.top{grid-template-columns:minmax(0,1fr) 72px}.companion,.pet-media{width:70px;height:70px}.pet-speech{right:57px;width:150px}.metrics{grid-template-columns:1fr 1fr}.new-preset{grid-template-columns:1fr}.new-preset button{width:100%}.composer-foot>span{display:none}}
  @media(max-height:620px){.top{min-height:96px}.pet-actions{display:none}.pet-media{width:72px;height:72px}.selection .quote{max-height:48px}}
  @media(prefers-reduced-motion:reduce){*,*::before,*::after{animation-duration:.001ms!important;animation-iteration-count:1!important;scroll-behavior:auto!important}.pet-media{transition:none}}
</style></head><body>
<div class="shell">
  <header class="top">
    <div class="top-copy"><p class="eyebrow">INKFLOW · 创作伙伴</p><h1>和墨宝一起写</h1><p>说目标、看过程、审结果。三个小说角色仍各守边界。</p></div>
    <div class="companion"><span class="pet-halo"></span><video id="petVideo" class="pet-media" muted playsinline autoplay loop disablepictureinpicture></video><img id="petImage" class="pet-media" alt="墨宝正在阅读选区" hidden><div id="petSpeech" class="pet-speech" role="status">我在。先告诉我今天想推进哪一步。</div></div>
    <div class="pet-actions" aria-label="与墨宝互动"><button data-pet="welcome">打招呼</button><button data-pet="reading">读选区</button><button data-pet="thinking">想一想</button><button data-pet="success">鼓励我</button><button data-pet="rest">休息</button></div>
  </header>
  <section id="empty" class="empty" hidden><p id="emptyNote">请先打开墨流小说项目。</p><button id="openFolder" class="primary">打开项目文件夹</button><button id="openDesktopEmpty">打开桌面版新建小说</button></section>
  <main id="workspace" class="content" hidden>
    <div class="scroll" id="scroll">
      <section class="status"><div class="status-head"><div><strong id="title">正在读取项目…</strong><small id="root"></small></div><button id="status" class="icon-button" title="查看完整项目状态">↗</button></div><div class="metrics"><div class="metric"><span>正史字数</span><b id="characters">0</b></div><div class="metric"><span>线索</span><b id="threads">0</b></div><div class="metric"><span>草稿</span><b id="drafts">0</b></div><div class="metric"><span>正史章</span><b id="accepted">0</b></div></div></section>
      <div class="section-title"><h2>当前会话</h2><span>答复以 ${revealRate} 字/秒淡入</span></div>
      <section id="messages" class="messages" aria-live="polite"></section>
      <section id="activity" class="activity" hidden><div class="activity-head"><span class="pulse"></span><strong id="activityLabel">正在理解目标</strong></div><ol id="timeline" class="timeline"></ol></section>
      <section id="selection" class="selection" hidden><div class="selection-head"><strong>已选中正文</strong><span id="selectionFile"></span></div><blockquote id="selectionQuote" class="quote"></blockquote><div class="chips"><button data-selection-prompt="让这段更具体，用可感知的动作或物件替代抽象说明。">更具体</button><button data-selection-prompt="收紧节奏，删除重复信息，但保留人物声线。">收紧节奏</button><button data-selection-prompt="让对白更自然、更符合人物关系和当下压力。">对白自然</button><button data-selection-prompt="加强因果衔接，不新增选区外事件。">补强因果</button></div><textarea id="selectionComment" placeholder="告诉 Writer 只改哪里、保留什么……"></textarea><div class="selection-actions"><button id="commentOnly" data-request>只留批注</button><button id="reviseSelection" class="primary" data-request>局部修订</button></div><p id="selectionNote" class="selection-note">局部修订只生成新草稿版本；不会自动验收或写入正史。</p></section>
      <details class="tray" id="presetTray"><summary>写作预设与快捷动作</summary><div class="tray-body"><div id="presetList" class="preset-list"></div><div class="new-preset"><input id="presetName" maxlength="20" placeholder="预设名称"><input id="presetPrompt" maxlength="1000" placeholder="固定提示内容"><button id="savePreset">添加</button></div></div></details>
      <details class="tray"><summary>章节工作流</summary><div class="tray-body"><div class="chapter-row"><input id="chapter" type="number" min="1" value="1" aria-label="章节号"></div><div class="workflow"><button data-action="plan" data-request>规划当前篇章<small>生成四级规划</small></button><button data-action="write" data-request>写本章草稿<small>只写草稿</small></button><button data-action="review" data-request>审查本章<small>查看证据与评分</small></button><button data-action="accept" data-request>验收进正史<small>需要再次确认</small></button></div></div></details>
      <details class="tray"><summary>项目工具</summary><div class="tray-body tool-row"><button id="refresh">刷新</button><button id="history">任务记录</button><button id="checkpoint">安全点</button><button id="desktop">桌面版</button><button id="mcp">模型工具</button><button id="askFirst">先问我</button></div></details>
      <p id="notice" class="notice"></p>
    </div>
    <form class="composer" id="composer"><textarea id="message" placeholder="直接说目标。例如：比较第 8～12 章章节卡，只指出风险，不要修改。"></textarea><div class="composer-foot"><span>Enter 发送 · Shift+Enter 换行</span><div class="composer-actions"><button id="stop" class="stop" type="button" hidden>停止</button><button id="send" class="primary" type="submit" data-request>发送</button></div></div></form>
  </main>
</div>
<script nonce="${nonce}">
  const vscode=acquireVsCodeApi();const ASSETS=${assets};const SETTINGS=${settings};const q=id=>document.getElementById(id);const restored=vscode.getState()||{};const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
  let messages=Array.isArray(restored.messages)?restored.messages.slice(-30):[{id:'welcome',role:'assistant',text:'我在这里。你可以先讨论，也可以让我规划、写草稿或审查；只有你明确验收后，内容才会进入正史。',reasoning:[],events:[]}];let presets=[];let selection=null;let activeEvents=[];let busy=false;let revealTimer=0;let speechTimer=0;let currentMood='idle';
  q('chapter').value=restored.chapter||1;q('message').value=restored.message||'';
  function persist(){vscode.setState({messages:messages.slice(-30),chapter:q('chapter').value,message:q('message').value})}function post(command,extra={}){vscode.postMessage({command,...extra})}function requestButtons(){return [...document.querySelectorAll('[data-request]')]}
  function setSpeech(text,hold=4200){clearTimeout(speechTimer);q('petSpeech').textContent=text;q('petSpeech').hidden=false;if(hold>0)speechTimer=setTimeout(()=>{q('petSpeech').hidden=true},hold)}
  function setMood(mood,phrase){currentMood=mood;const video=q('petVideo'),image=q('petImage');if(mood==='reading'){video.pause();video.hidden=true;image.hidden=false;image.src=ASSETS.reading}else{image.hidden=true;video.hidden=false;const clip=ASSETS.clips[mood]||ASSETS.clips.idle;if(video.dataset.mood!==mood){video.dataset.mood=mood;video.src=clip.video;video.poster=clip.poster;video.loop=!['success','welcome'].includes(mood);video.load()}if(!document.hidden&&!reduced)video.play().catch(()=>{})}if(phrase)setSpeech(phrase)}
  function petPhrase(mood){return({welcome:'你好，我是墨宝。今天从灵感、正文还是审查开始？',reading:selection?'我已经盯住这段文字了。告诉我只改什么。':'先在 Markdown 正文里选一段，我就能帮你批注或局部修订。',thinking:'我会先核对目标和边界，再决定下一步。',success:'慢慢写也算向前。每一版都可以回看。',rest:'我先趴一会儿；你随时可以继续。',idle:'我在。'})[mood]}
  document.querySelectorAll('[data-pet]').forEach(button=>button.onclick=()=>{const mood=button.dataset.pet;setMood(mood,petPhrase(mood));if(!['idle','rest'].includes(mood))setTimeout(()=>{if(!busy&&currentMood===mood)setMood(selection?'reading':'idle')},4300)});
  function finishReveal(){if(!revealTimer)return;const state=window.__reveal;clearInterval(revealTimer);revealTimer=0;if(state){state.node.textContent=state.text;state.skip.remove();window.__reveal=null}}
  function reveal(node,text,skip){finishReveal();if(skip||reduced){node.textContent=text;return}const characters=Array.from(text);let index=0;node.replaceChildren();const button=document.createElement('button');button.className='skip-reveal';button.textContent='立即显示';button.onclick=finishReveal;node.after(button);window.__reveal={node,text,skip:button};const delay=Math.max(16,Math.round(1000/SETTINGS.revealCharactersPerSecond));revealTimer=setInterval(()=>{const last=document.createElement('span');last.className='reveal-char';last.textContent=characters[index]||'';node.append(last);index+=1;if(index>=characters.length)finishReveal()},delay)}
  function messageNode(message,animate=false){const article=document.createElement('article');article.className='message '+message.role+(message.error?' error':'');const avatar=document.createElement('span');avatar.className='avatar';avatar.textContent=message.role==='user'?'你':message.role==='system'?'记':'墨';const bubble=document.createElement('div');bubble.className='bubble';const text=document.createElement('p');text.className='message-text';text.setAttribute('aria-label',message.text);bubble.append(text);const reasoning=Array.isArray(message.reasoning)?message.reasoning:[];const events=Array.isArray(message.events)?message.events:[];if(reasoning.length||events.length||message.details){const details=document.createElement('details');const summary=document.createElement('summary');summary.textContent='查看判断摘要与过程'+(reasoning.length?' · '+reasoning.length+' 条':'');details.append(summary);if(reasoning.length){const list=document.createElement('ol');list.className='reasoning-list';reasoning.forEach(item=>{const li=document.createElement('li');li.textContent=item;list.append(li)});details.append(list)}if(events.length){const list=document.createElement('ol');list.className='reasoning-list';events.forEach(item=>{const li=document.createElement('li');li.textContent=item;list.append(li)});details.append(list)}if(message.details){const pre=document.createElement('pre');pre.textContent=message.details;details.append(pre)}bubble.append(details)}article.append(avatar,bubble);reveal(text,message.text,animate);return article}
  function renderMessages(){q('messages').replaceChildren(...messages.map(item=>messageNode(item,false)))}function appendMessage(role,text,extra={},animate=false){const item={id:crypto.randomUUID(),role,text,...extra};messages.push(item);messages=messages.slice(-30);q('messages').append(messageNode(item,animate));persist();requestAnimationFrame(()=>q('scroll').scrollTo({top:q('scroll').scrollHeight,behavior:reduced?'auto':'smooth'}))}
  function setBusy(value,label){busy=value;requestButtons().forEach(button=>button.disabled=value);q('stop').hidden=!value;q('activity').hidden=!value;if(label){q('activityLabel').textContent=label;setSpeech(label,0)}if(value){activeEvents=[];q('timeline').replaceChildren();setMood('thinking')}else if(currentMood==='thinking'){setMood(selection?'reading':'idle')}}
  function renderTimeline(){q('timeline').replaceChildren(...activeEvents.slice(-8).map(item=>{const li=document.createElement('li');li.textContent=item;return li}))}
  function renderSelection(){const card=q('selection');if(!selection){card.hidden=true;if(!busy&&currentMood==='reading')setMood('idle');return}card.hidden=false;q('selectionFile').textContent=selection.label||selection.relativePath||'';q('selectionQuote').textContent=selection.quote||'';q('reviseSelection').disabled=busy||selection.dirty||selection.tooLong||!selection.isDraft;q('commentOnly').disabled=busy||selection.dirty;q('selectionNote').textContent=selection.dirty?'请先保存正文，再对稳定版本批注或修订。':selection.tooLong?'选区超过 8000 字：可以留批注，局部修订请缩小范围。':selection.isDraft?'局部修订只生成新草稿版本；不会自动验收或写入正史。':'这不是未验收草稿：可以留批注，但正史修改必须先预览影响并确认。';if(!busy)setMood('reading','我读到你选中的这段了。想保留什么、只改什么？')}
  function renderPresets(){const nodes=presets.map(item=>{const wrap=document.createElement('span');wrap.className='preset';const use=document.createElement('button');use.textContent=item.label;use.title=item.prompt;use.onclick=()=>{q('message').value=item.prompt;q('message').focus();persist();setSpeech('预设已经放进输入框，你可以继续改。')};wrap.append(use);if(!item.builtin){const del=document.createElement('button');del.className='delete';del.textContent='×';del.title='删除这个自定义预设';del.onclick=()=>post('deletePreset',{id:item.id});wrap.append(del)}return wrap});q('presetList').replaceChildren(...nodes)}
  function submitMessage(text,label){const value=(text||'').trim();if(!value||busy)return;appendMessage('user',value);q('message').value='';persist();setBusy(true,label||'正在理解你的目标');post('send',{text:value})}
  q('composer').onsubmit=event=>{event.preventDefault();submitMessage(q('message').value)};q('message').onkeydown=event=>{if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();q('composer').requestSubmit()}};q('message').oninput=persist;q('stop').onclick=()=>post('cancel');
  q('openFolder').onclick=()=>post('openFolder');q('openDesktopEmpty').onclick=()=>post('openDesktop');q('status').onclick=()=>post('openStatus');q('refresh').onclick=()=>post('refresh');q('history').onclick=()=>post('openTaskHistory');q('checkpoint').onclick=()=>post('createCheckpoint');q('desktop').onclick=()=>post('openDesktop');q('mcp').onclick=()=>post('configureMcp');
  q('askFirst').onclick=()=>submitMessage('先不要执行任务。请根据当前项目状态和最近讨论，用选项主动问我一到三个最值得确认的问题；说明答案会影响什么，最后保留其他选项。','正在准备关键问题');
  document.querySelectorAll('[data-action]').forEach(button=>button.onclick=()=>{const chapter=Number(q('chapter').value||0),action=button.dataset.action;const labels={plan:'生成四级规划',write:'写第 '+chapter+' 章草稿',review:'审查第 '+chapter+' 章',accept:'验收第 '+chapter+' 章进入正史'};appendMessage('user',labels[action]||'运行章节工作流');setBusy(true,'任务已提交');post('workflow',{action,chapter})});q('chapter').onchange=persist;
  document.querySelectorAll('[data-selection-prompt]').forEach(button=>button.onclick=()=>{q('selectionComment').value=button.dataset.selectionPrompt;q('selectionComment').focus()});
  function selectionAction(revise){const instruction=q('selectionComment').value.trim();if(!instruction){setSpeech('先写一句具体修改要求。');q('selectionComment').focus();return}appendMessage('user',(revise?'局部修订：':'选区批注：')+instruction);setBusy(true,revise?'Writer 正在处理选区':'正在保存批注');post('selectionAction',{revise,instruction})}q('commentOnly').onclick=()=>selectionAction(false);q('reviseSelection').onclick=()=>selectionAction(true);
  q('savePreset').onclick=()=>{const label=q('presetName').value.trim(),prompt=q('presetPrompt').value.trim();if(!label||!prompt){setSpeech('预设需要一个短名称和完整提示。');return}post('savePreset',{label,prompt});q('presetName').value='';q('presetPrompt').value=''};
  document.addEventListener('visibilitychange',()=>{const video=q('petVideo');if(document.hidden)video.pause();else if(!reduced&&!video.hidden)video.play().catch(()=>{})});
  window.addEventListener('message',event=>{const data=event.data||{};if(data.type==='busy'){setBusy(true,data.label);return}if(data.type==='progress'){const summary=String(data.summary||'正在处理');if(!activeEvents.includes(summary))activeEvents.push(summary);renderTimeline();q('activityLabel').textContent=summary;setSpeech(summary,0);return}if(data.type==='result'){setBusy(false);appendMessage('assistant',String(data.text||'任务已完成。'),{reasoning:data.reasoning||[],events:activeEvents.slice(),details:data.details||''},!data.error);setMood(data.error?'rest':'success',data.error?'这一步没有完成，原因已经放在对话里。':'完成了。你可以先读结果，再决定下一步。');activeEvents=[];return}if(data.type==='cancelled'){setBusy(false);appendMessage('assistant','当前任务已停止。已经落盘的草稿会保留，未验收内容不会进入正史。',{events:activeEvents.slice()});setMood('waiting','已停下，等你决定下一步。');activeEvents=[];return}if(data.type==='selection'){selection=data.selection||null;renderSelection();return}if(data.type==='presets'){presets=Array.isArray(data.presets)?data.presets:[];renderPresets();return}if(data.type==='state'){q('empty').hidden=Boolean(data.ready);q('workspace').hidden=!data.ready;if(!data.ready){q('emptyNote').textContent=data.note||'请先打开墨流小说项目。';setMood('welcome');return}q('title').textContent=data.title||'墨流小说';q('root').textContent=data.root||'';q('characters').textContent=Number(data.acceptedCharacters||0).toLocaleString();q('threads').textContent=String(data.openThreads||0);q('drafts').textContent=String(data.draftChapters||0);q('accepted').textContent=String(data.acceptedChapters||0);q('notice').textContent=data.error||data.note||(data.planned?'四级规划已就绪。':'尚未生成四级规划。')}});
  renderMessages();setMood('idle');post('ready');
</script></body></html>`;
}

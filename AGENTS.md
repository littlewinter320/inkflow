# 墨流（InkFlow）仓库维护规范

本文件只保留会影响后续维护和公开发布的长期规则。产品名固定写作“墨流（InkFlow）”，不要把版本号拼进产品名。

## README 规范

根目录 `README.md` 是公开仓库的统一入口，面向普通使用者，不是开发日志、需求池或历史方案存档。每次修改 README 时遵守以下顺序：

1. 第一屏说明墨流解决什么问题、适合谁，并提供最新正式版下载入口、全部版本入口和问题反馈入口。
2. 先介绍可使用的产品模块，再单独介绍系统架构；模块介绍回答“能做什么”，架构介绍回答“由谁负责、怎样连接”。
3. 把“完整创作流程”单独成节，按实际运行顺序说明讨论、规划、草稿、审查、验收和正史提交；不要把架构图当成流程图。
4. 使用表格、流程图和短例子解释四个正式 Agent、Context Packet、版本状态、语音边界和正史门禁。普通用户不需要先理解 Python、MCP、RAG 或 SQLite。
5. 只把当前代码能够证明的能力写成“已支持”。计划中的功能放到 Roadmap 或 Issue，不能写成已经完成；版本号、下载链接、支持平台和模型名称必须与代码和 Release 一致。
6. README 可以包含简短的版本亮点、安装、常见问题、隐私和源码运行说明，但不放一次性研究、内部讨论、旧发布日志、个人路径、测试小说、模型密钥或无运行入口的设计文档。
7. 语音朗读稿（Writer 生成 180～400 字）和参考录音（Voice Runtime 3～120 秒）必须作为两个独立约束说明，不能合并成一个模糊的“声音克隆步骤”。

README 改完后，检查标题层级、链接、版本状态和 Mermaid 图是否仍然能单独阅读。除非用户明确要求，不为 README 修改额外创建重复说明文件。

## 产品边界

墨流固定使用四个正式 AI Agent：

- Coordinator：理解自然语言、拆解目标、生成任务单、选择固定工作流、安排协作和汇总结果；不能写正文、替 Reviewer 放行或替 Memory Keeper 提交正史。
- Writer：负责规划、创意、草稿和定点修订。
- Reviewer：只审查当前版本，必须给出正文或 Context Packet 证据，不直接修改正文。
- Memory Keeper：只从用户已接受且通过审查的正文提取正史补丁。

Novel Engine、Context Builder、检索器、Provider Adapter 和 Voice Runtime 是确定性组件，不计入 Agent 数量，也不能绕过权限门禁。草稿、审查报告、工作资料和正史必须保持可区分；用户只要求草稿时，不自动验收或写入正史。

## 发布与下载

- Python 引擎、桌面端和需要同步的扩展版本必须一致；标签、Release 标题、安装包文件名和 README 版本状态必须一致。
- 正式 Windows Release 至少包含安装程序、`latest.yml` 和 `.exe.blockmap`；README 使用 `releases/latest` 作为稳定下载页入口，不硬编码会过期的版本文件名，除非同时保留版本历史链接。
- Release 更新说明应说明改动、用户可见效果、兼容影响和已知限制，不把构建日志当更新说明。
- 只有当前请求明确授权时才能推送标签、创建或修改 Release、上传安装包或发布流水线。
- GitHub 只保留运行、构建、安装、宿主接入、许可证和当前必要说明直接需要的文件；开发工具配置、历史计划、一次性研究和重复文档留在本地或删除。

## 安全和隐私

API Key 只能来自环境变量或系统凭据库，不得写入代码、README、Git、日志、Trace 或小说文件。用户小说、录音和声音档案默认保存在本地项目或应用语音目录；公开文档不得包含私人路径和真实模型密钥。
